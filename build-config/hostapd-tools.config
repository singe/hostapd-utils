# Shared hostapd build profile for ifcap and hapdconf-check.
#
# Keep this overlay deliberately small. The Alpine Linux snapshot supplies the
# distro-style feature baseline; DEBUG_FILE keeps hostapd diagnostics off each
# utility's machine-readable stdout stream.
include $(HOSTAPD_ALPINE_CONFIG)
CONFIG_DEBUG_FILE=y
CONFIG_HS20=y
